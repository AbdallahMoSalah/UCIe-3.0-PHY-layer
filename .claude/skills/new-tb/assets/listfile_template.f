# =============================================================================
# Listfile: __CONFIG__.f
# Purpose : <one line — what this TB exercises>
#
# Run : make run CONFIG=__CONFIG__ TOP=__TB__
# =============================================================================

# ── RTL dependencies (packages BEFORE the modules that import them) ───────────
# rtl/.../<dep_pkg>.sv
# rtl/.../<dep>.sv

# ── DUT ──────────────────────────────────────────────────────────────────────
# rtl/.../__DUT__.sv

# ── Testbench (last) ─────────────────────────────────────────────────────────
# tb/.../__TB__.sv
